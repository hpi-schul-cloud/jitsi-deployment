apiVersion: apps/v1
kind: StatefulSet
metadata:
  namespace: jitsi
  name: jvb
spec:
  template:
    spec:
      containers:
        - name: prometheus-exporter
          resources:
            requests:
              cpu: "50m"
              memory: "50Mi"
            limits:
              cpu: "100m"
              memory: "100Mi"
        - name: jvb
          resources:
            requests:
              cpu: "4400m"
              memory: "1000Mi"
            limits:
              cpu: "4800m"
              memory: "1500Mi"
